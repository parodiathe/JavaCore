*/
3. Задача по алгоритмам.

Задача: Написать метод, который возвращает минимальное и максимальное числа в массиве. 
/*


import java.util.*;

public class Main {

    public static void main(String[] args) {
        Scanner keyboard = new Scanner(System.in);
        int[] array = new int[5];
        for (int i = 0; i < array.length; i++) {
            array[i] = keyboard.nextInt();
        }
        Arrays.sort(array);
        for (int i = 0; i < 1; i++){
            System.out.println("Минимальный элемент = " + array[0]);
            System.out.println("Максимальный элемент = " + array[array.length -1]);
        }
    }
}
